package com.example.muntugo; // Remplacez par votre package

import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;

public class BusReservationActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_bus_reservation); // Assurez-vous que le nom du fichier XML est correct

        // Logique de réservation de bus ici
    }
}